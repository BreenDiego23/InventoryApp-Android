<!-- AndroidManifest.xml declares essential information about the app to the Android build tools, the Android operating system, and Google Play. -->
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to send SMS messages -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Telephony feature, not required but used if available -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- Declares global application settings -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryApp"
        tools:targetApi="31">

        <!-- Login screen activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inventory screen activity -->
        <activity android:name=".InventoryActivity" />
        <!-- Add item screen activity -->
        <activity android:name=".AddItemActivity" />
        <!-- SMS notification screen activity -->
        <activity android:name=".SmsNotificationActivity" />

    </application>

</manifest>